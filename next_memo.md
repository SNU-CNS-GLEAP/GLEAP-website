# 다음 웹사이트 만들 때 참고할 메모

GLEAP 홈페이지 만들면서 겪은 시행착오 모음. 프로젝트 특정 내용이 아니라
**다음에 다른 사이트를 만들 때도 적용될 만한 교훈**만 짧게 정리.

---

## 배포 / 인프라

- **함수 리전은 처음부터 사용자 지역에 맞춰라.** Vercel Hobby 기본 리전은
  워싱턴 D.C.(`iad1`). 한국 사용자 대상이면 `vercel.json`에
  `{"regions": ["icn1"]}`을 프로젝트 시작 시점에 넣어둘 것 — 나중에 "왜 느리지"
  하고 원인을 찾느니 처음부터 맞추는 게 싸다.
- **DB 리전도 함수 리전과 맞춰라.** 특히 Neon처럼 **리전을 나중에 못 바꾸는
  서비스**는 만들기 전에 결정해야 함. 함수는 서울인데 DB가 미국이면, 이번엔
  함수→DB 왕복에서 같은 지연이 재발한다.
- **로그인/개인화 없는 페이지는 무조건 정적으로 만들 수 있다.** "DB에서
  데이터를 읽어온다" ≠ "동적이어야 한다". 쿠키·세션처럼 요청마다 달라지는
  게 있는 페이지만 동적으로 남기고, 나머지는 정적 생성 + 주기적/수동
  재검증(ISR)으로 처리.

## i18n

- **next-intl 자체 선택은 좋았음.** 문제는 설정 누락 쪽이었음.
- **로케일 라우팅 라이브러리를 쓰면 "기본 설정만으로는 전부 동적 렌더링될 수
  있다"는 걸 처음부터 의심하고 확인할 것.** next-intl은 요청 헤더에서 locale을
  읽는 방식이 기본이라, `generateStaticParams` + `setRequestLocale`을 명시적으로
  안 넣으면 `[locale]` 하위 전체가 서버 렌더링으로 빠진다. 배포 후 빌드 로그의
  `○`/`●`/`ƒ` 표시를 꼭 확인.
- **"하나라도 빠뜨리면 그 아래 전체가 동적으로 남는다" 류의 규칙은 페이지
  추가할 때마다 재발한다.** 새 페이지 만들 때 체크리스트에 넣어두는 게 나음.

## 환경변수

- **`$`로 시작하는 값(bcrypt 해시 등)을 `.env.local`에 넣을 때 주의.**
  Next.js가 로컬에서 dotenv-expand로 `$2b`, `$10` 같은 부분을 변수 참조로
  오인해서 잘라먹는다. 로컬에서만 `\$`로 이스케이프하고, Vercel 등 다른 곳에
  값을 넣을 땐 원본 그대로 넣어야 함 — **이스케이프한 값을 그대로 복사하면 안 됨.**
- **`.gitignore`의 `.env*` 같은 와일드카드가 `.env.example`까지 막을 수 있다.**
  예시 파일은 커밋 대상이므로 `!.env.example`로 명시적으로 풀어줘야 함.
  프로젝트 시작할 때 한 번 확인.

## 메이저 버전 업그레이드

- **새 프레임워크 버전을 쓸 땐 "알고 있는 지식과 다를 수 있다"고 가정하고
  로컬 문서부터 확인.** Next.js 16에서 `middleware.ts`가 `proxy.ts`로 이름이
  바뀐 것처럼, 학습 데이터에 없는 변경이 있을 수 있음. `node_modules/*/dist/docs`
  같은 실제 설치된 버전의 문서를 먼저 보는 습관.
- **서버 컴포넌트 API도 동기/비동기 버전이 갈릴 수 있다.** next-intl의
  `useTranslations`는 async 컴포넌트에서 못 쓰고, `next-intl/server`의
  `getTranslations`(비동기 버전)를 따로 써야 했음.

## 설계 원칙

- **자주 안 바뀌는 콘텐츠는 DB에 넣지 마라.** 소개/구성원처럼 1년에 몇 번 안
  바뀌는 건 코드 데이터 파일로 두고 "고치고 git push"로 끝내는 게, DB 스키마
  + 관리자 편집 UI를 만드는 것보다 훨씬 쌈.
- **번역이 필요한 구조화된 데이터는 언어별 파일 분리 대신 항목 안에 나란히.**
  `{ ko, en }` 필드를 같은 항목에 두면 두 언어의 항목 개수·순서가 어긋날 일이
  없음. 별도 파일(`ko.json`/`en.json`)로 쪼개는 건 UI 문구처럼 항목이 고정된
  경우에만.
